<?php 

require '../../boot.php';
require '../../session.php';

if (! isset($session) || $session->auth == "") {
    Common::Error(401, 'json');
}

$urusan = DB::get("urusan");

echo json_encode($urusan);

exit;